{{node-credentials-form
  infoMessage=infoMessage
  errorMessage=errorMessage
  isProcessing=isProcessing
  nodeIdPlaceholder=(t 'login.nodeId.placeholder')
  submitTitle=(t 'login.submit')
  onSubmit=(action 'authenticate')}}
