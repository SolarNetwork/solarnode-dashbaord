  {{#liquid-if hasMessage class="app-message"}}
    {{#if errorMessage}}
      <div class="uk-alert uk-alert-warning">
        <p><strong>{{t "error.intro"}}</strong> {{errorMessage}}</p>
      </div>
    {{/if}}

    {{#if infoMessage}}
      <div class="uk-alert uk-alert-success">
        <p>{{infoMessage}}</p>
      </div>
    {{/if}}
  {{/liquid-if}}

<form class="uk-form uk-form-stacked uk-panel uk-panel-box uk-panel-box-primary"
    {{action 'onSubmit' on='submit'}}>
    <fieldset class="uk-width-1-1">
      <div class="uk-form-row">
        <label for="identification" class="uk-form-label">{{t 'login.nodeId.label'}}</label>
        <div class="uk-form-controls">
          {{input id='identification' value=identification placeholder=nodeIdPlaceholder class='uk-width-1-1'}}
        </div>
      </div>
      {{#liquid-if showToken class="show-token"}}
        <div class="uk-form-row">
          <label for="token" class="uk-form-label">{{t 'login.authToken.label'}}</label>
          <div class="uk-form-controls">
            {{input id='token' value=token placeholder=(t 'login.authToken.placeholder') class='uk-width-1-1'}}
          </div>
        </div>
        <div class="uk-form-row">
          <label for="secret" class="uk-form-label">{{t 'login.authSecret.label'}}</label>
          <div class="uk-form-controls">
            {{input id='secret' value=secret placeholder=(t 'login.authSecret.placeholder') type='password' class='uk-width-1-1'}}
          </div>
        </div>
      {{else}}
        <div class="uk-form-row">
          <label>
            {{input type="checkbox" name="showToken" checked=showToken}}
            {{t "login.showToken"}}
          </label>
        </div>
      {{/liquid-if}}
    </fieldset>
	<br>
	<button type="submit" class="uk-button uk-button-primary uk-width-1-1" disabled={{isProcessing}}>{{submitTitle}}</button>
</form>
