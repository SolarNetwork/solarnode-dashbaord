{{node-credentials-form
  infoMessage=infoMessage
  errorMessage=errorMessage
  isProcessing=isProcessing
  nodeIdPlaceholder=(t 'data-props.addNode.nodeIdPlaceholder')
  submitTitle=(t 'data-props.addNode.submit')
  onSubmit=(action 'addNode')}}
